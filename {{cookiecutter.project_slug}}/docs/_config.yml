# GitHub Pages builds this branch with Jekyll. Only presentation lives here;
# every page under cli/ and api/ is generated by `make docs`.
title: {{ cookiecutter.project_name }}
description: {{ cookiecutter.project_description }}
theme: jekyll-theme-minimal
show_downloads: false
