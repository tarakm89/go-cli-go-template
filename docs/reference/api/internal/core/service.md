---
layout: "doc"
nav_id: "reference"
eyebrow: "Package"
title: "internal/core/service"
breadcrumb_section: "Reference"
breadcrumb_url: "/docs/reference/index.html"
doc_generated: true
doc_generator: "gomarkdoc"
doc_prev_url: "/docs/reference/api/internal/core/port.html"
doc_prev_title: "internal/core/port"
doc_next_url: "/docs/reference/api/internal/observability.html"
doc_next_title: "internal/observability"
---

{% raw %}
<!-- Code generated by gomarkdoc. DO NOT EDIT -->

```go
import "github.com/acme/probe-cli/internal/core/service"
```

Package service implements the inbound ports. It orchestrates domain rules and outbound ports and is the only place where a use case lives.

It depends on domain and port. It must never import an adapter — the depguard rule in .golangci.yml enforces that.

## Index

- [type Health](<#Health>)
  - [func NewHealth\(prober port.Prober, opts ...Option\) \*Health](<#NewHealth>)
  - [func \(h \*Health\) Check\(ctx context.Context, target domain.Target\) domain.Health](<#Health.Check>)
  - [func \(h \*Health\) CheckAll\(ctx context.Context, targets \[\]domain.Target\) \[\]domain.Health](<#Health.CheckAll>)
- [type Option](<#Option>)
  - [func WithConcurrency\(n int\) Option](<#WithConcurrency>)
  - [func WithThresholds\(t domain.Thresholds\) Option](<#WithThresholds>)


<a name="Health"></a>
## type Health

Health is the use case behind the \`check\` command.

```go
type Health struct {
    // contains filtered or unexported fields
}
```

<a name="NewHealth"></a>
### func NewHealth

```go
func NewHealth(prober port.Prober, opts ...Option) *Health
```

NewHealth wires the use case to an outbound prober.

<a name="Health.Check"></a>
### func \(\*Health\) Check

```go
func (h *Health) Check(ctx context.Context, target domain.Target) domain.Health
```

Check inspects a single target.

<a name="Health.CheckAll"></a>
### func \(\*Health\) CheckAll

```go
func (h *Health) CheckAll(ctx context.Context, targets []domain.Target) []domain.Health
```

CheckAll inspects every target concurrently, preserving input order. A cancelled context is not an error either: the targets that did not get a verdict come back as unreachable, which is what the user needs to see.

<a name="Option"></a>
## type Option

Option customises a Health service.

```go
type Option func(*Health)
```

<a name="WithConcurrency"></a>
### func WithConcurrency

```go
func WithConcurrency(n int) Option
```

WithConcurrency caps how many targets are probed at once. Values below one are ignored.

<a name="WithThresholds"></a>
### func WithThresholds

```go
func WithThresholds(t domain.Thresholds) Option
```

WithThresholds overrides the latency budget used to grade responses.

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
{% endraw %}
