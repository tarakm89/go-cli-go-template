# Jekyll output
_site/
.jekyll-cache/
.jekyll-metadata

# Generated at build time from CHANGELOG.md on main by
# scripts/inject-changelog.py — never committed, so main's changelog stays the
# single source of truth for what has been released.
_data/versions.json

# Where the Pages workflow checks main's changelog out.
.main/

.DS_Store
